# Site settings for the ECC home page.
#
# This page is intentionally self-contained: it does NOT use a remote Jekyll
# theme. All styling lives in assets/css/style.scss and the layout in
# _layouts/default.html, so the CSS always ships with the site and renders
# correctly when served at the site root on a custom domain.

title: ECC
description: A Lean 4 formalization of Error Correcting Codes
author: Alex Chai, Anthony Chang, Erin Jaen, and Henry Yuen
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://ecc.wurtle.dev"
github_username: wurtylex
repository: wurtylex/LeanECC

# Build settings
plugins:
  - jekyll-seo-tag
